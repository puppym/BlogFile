1. 打印有限域的值

```
                    println!("*******************");
                    println!("parent_value_vec: {:?}", parent_value_vec[j].unwrap());
                    println!("sibling_value_vec: {:?}", sibling_value_vec[j].unwrap());
                    println!("input_value: {:?}", input_value[j].unwrap());
                    println!("is_left_value: {:?}", is_left_value.unwrap());
                    println!("F::zero(): {:?}", F::zero());
                    println!("F::one(): {:?}", F::one());
                    println!("*******************");
```

2. Boolean 类型转换为 Fr类型

```

```

